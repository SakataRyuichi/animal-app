# mise + Convex + Turborepo 設計レビュー

このドキュメントは、提供された意見を参考に、現在の設計（mise、Convex、Turborepo）に不都合や問題がないかを検証した結果です。

**検証日**: 2026年2月1日  
**検証結果**: ✅ **設計は概ね良好。いくつかの設定ファイルを作成・確認が必要。**

---

## 検証サマリー

| 項目 | 状態 | 備考 |
|------|------|------|
| ディレクトリ構成 | ✅ 良好 | `packages/backend/`にConvexを配置 |
| 不都合A: 依存関係の競合 | ✅ 対策済み | `pnpm-workspace.yaml`で分離 |
| 不都合B: `convex dev`のキャッシュ | ✅ 対策済み | `turbo.json`で`cache: false`を指定 |
| 不都合C: 型定義の共有 | ✅ 対策済み | `exports`フィールドでエクスポート |
| mise設定 | ✅ 良好 | ルートで統一管理 |
| `turbo.json` | ✅ 作成完了 | `dev`タスクに`cache: false`を指定 |
| `pnpm-workspace.yaml` | ✅ 作成完了 | ワークスペース設定を実装 |
| `convex.json` | ✅ 作成完了 | Convex設定ファイルを実装 |
| `packages/backend/package.json` | ⚠️ 確認必要 | テンプレートを作成済み |

## 提供された意見の要点

1. **ConvexとReact NativeのNode環境を分けることは可能で、Turborepoではそれが「正解」**
2. **不都合A**: `node_modules`の依存関係の競合 → 対策: Turborepoのワークスペース機能で分離
3. **不都合B**: `convex dev`とTurborepoのキャッシュ → 対策: `turbo.json`で`cache: false`を指定
4. **不都合C**: 型定義の共有 → 解決策: `convex/_generated/api.d.ts`を参照できるようにエイリアスを貼る
5. **推奨ディレクトリ構成**: `apps/mobile/`と`convex/`を分離

---

## 現在の設計の検証結果

### ✅ 1. ディレクトリ構成（概ね良好、一部改善推奨）

**現在の構成**:
```
animal-app/
├── apps/
│   ├── expo/          # React Native Expo アプリ
│   ├── admin/         # Next.js 管理画面
│   └── www/           # Next.js 公式サイト
├── packages/
│   ├── backend/       # Convexバックエンド（独立パッケージ）
│   │   └── convex/    # スキーマ、関数、AIアクション
│   ├── ui/            # 共通UIコンポーネント
│   ├── utils/         # 共通ロジック
│   └── tsconfig/      # TypeScript共通設定
└── .mise.toml         # mise設定
```

**推奨構成との比較**:
- ✅ `packages/backend/`にConvexを配置している → **正しい**
- ⚠️ 意見では`convex/`をルートに置くことを推奨しているが、`packages/backend/`でも問題なし
- ✅ 独立パッケージとして分離されている → **正しい**

**結論**: 現在の構成で問題なし。`packages/backend/`の方がモノレポの一貫性が保たれる。

---

### ⚠️ 2. 不都合A: `node_modules`の依存関係の競合（対策が必要）

**問題点**:
- Convexはバックエンドなので`node-fetch`や`lucide-react`（Web用）などを使いたい
- React Nativeはモバイル用ライブラリを必要とする
- 依存関係が競合する可能性がある

**現在の状況**:
- `packages/backend/`が独立パッケージとして配置されている → ✅ **良好**
- ただし、`pnpm-workspace.yaml`と`turbo.json`の設定を確認する必要がある

**推奨対策**:
1. `pnpm-workspace.yaml`で`packages/backend/`を独立したワークスペースとして定義
2. `packages/backend/package.json`でConvex専用の依存関係を管理
3. `apps/expo/package.json`でReact Native専用の依存関係を管理

**必要な設定ファイル**:
- `pnpm-workspace.yaml`（確認・作成が必要）
- `turbo.json`（確認・作成が必要）
- `packages/backend/package.json`（確認が必要）

---

### ⚠️ 3. 不都合B: `convex dev`とTurborepoのキャッシュ（対策が必要）

**問題点**:
- Turborepoは「入力が変わらなければキャッシュから出す」のが基本
- `convex dev`は常に動的にバックエンドを同期し続ける
- キャッシュによってConvexの同期がスキップされる可能性がある

**現在の状況**:
- `turbo.json`が存在しない → ⚠️ **作成が必要**

**推奨対策**:
`turbo.json`で`dev`タスクに`cache: false`を指定：

```json
{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", ".expo/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "lint": {
      "outputs": []
    }
  }
}
```

**特に重要な設定**:
- `packages/backend`の`dev`タスク（`convex dev`）は`cache: false`を指定
- `persistent: true`で常時実行されることを明示

---

### ✅ 4. 不都合C: 型定義の共有（現在の設計で対応可能）

**問題点**:
- React Native側で`useQuery(api.myFunction)`と書く際、Convex側の型定義を参照する必要がある
- `convex/_generated/api.d.ts`を参照できるようにする必要がある

**現在の状況**:
- `packages/backend/convex/_generated/api.d.ts`が生成される → ✅ **良好**
- `apps/expo`から`@repo/backend`として参照できる → ✅ **良好**

**推奨対策**:
1. `packages/backend/package.json`で`exports`フィールドを設定：
   ```json
   {
     "name": "@repo/backend",
     "exports": {
       "./convex/_generated/api": "./convex/_generated/api.d.ts",
       "./convex/_generated/api.d.ts": "./convex/_generated/api.d.ts"
     }
   }
   ```

2. `apps/expo/tsconfig.json`でパスエイリアスを設定：
   ```json
   {
     "compilerOptions": {
       "paths": {
         "@repo/backend/convex/_generated/api": ["../../packages/backend/convex/_generated/api"]
       }
     }
   }
   ```

3. 使用例：
   ```typescript
   // apps/expo/app/home.tsx
   import { api } from "@repo/backend/convex/_generated/api";
   import { useQuery } from "convex/react";
   
   const pets = useQuery(api.pets.get); // 型安全！
   ```

**現在の設計**: 既に`@repo/backend`として参照できる設計になっている → ✅ **良好**

---

### ⚠️ 5. mise設定の検証（改善推奨）

**現在の`.mise.toml`**:
```toml
[tools]
node = "20.18.0"
pnpm = "9.12.3"
```

**推奨改善**:
1. Convexは`convex.json`でクラウド上のバージョンを指定するため、ローカルのNode.jsバージョンに縛られない
2. ただし、ローカル開発環境の統一のため、miseでNode.jsバージョンを管理するのは正しい
3. `packages/backend/.mise.toml`で個別にバージョンを上書きすることも可能だが、通常は不要

**結論**: 現在の設定で問題なし。ルートの`.mise.toml`で統一管理するのが適切。

---

### ⚠️ 6. Convex設定ファイルの確認（作成が必要）

**必要なファイル**:
- `packages/backend/convex.json` → ⚠️ **確認・作成が必要**
- `packages/backend/package.json` → ⚠️ **確認が必要**

**推奨設定**:

**`packages/backend/convex.json`**:
```json
{
  "functions": "convex/",
  "generateCommonJSApi": false,
  "node": {
    "externalPackages": []
  }
}
```

**`packages/backend/package.json`**:
```json
{
  "name": "@repo/backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "convex": "^1.15.0"
  },
  "devDependencies": {
    "@types/node": "^20.18.0",
    "typescript": "^5.6.0"
  }
}
```

---

## 総合評価と推奨事項

### ✅ 良好な点

1. **ディレクトリ構成**: `packages/backend/`にConvexを独立パッケージとして配置 → **正しい**
2. **型定義の共有**: `@repo/backend`として参照できる設計 → **正しい**
3. **mise設定**: ルートで統一管理 → **正しい**

### ✅ 改善完了した点

1. **`turbo.json`の作成**: ✅ **完了** - `convex dev`のキャッシュ対策を実装
2. **`pnpm-workspace.yaml`の作成**: ✅ **完了** - ワークスペース設定を実装
3. **`convex.json`の作成**: ✅ **完了** - Convex設定ファイルを実装
4. **`packages/backend/package.json`の確認**: ⚠️ **確認が必要** - 依存関係の管理方法の確認が必要

### ✅ 実装済みの設定ファイル

1. **`turbo.json`**: ✅ **作成完了**
   - `dev`タスクに`cache: false`を指定（`convex dev`のキャッシュ対策）
   - `persistent: true`で常時実行を明示

2. **`pnpm-workspace.yaml`**: ✅ **作成完了**
   - `packages/backend/`を独立したワークスペースとして定義

3. **`convex.json`**: ✅ **作成完了**
   - Convex設定ファイルを`packages/backend/convex.json`に配置

4. **`packages/backend/package.json.example`**: ✅ **テンプレート作成完了**
   - `exports`フィールドで型定義をエクスポート
   - Convex専用の依存関係を管理

### 🚀 次のステップ（実装が必要）

1. **`packages/backend/package.json`の確認・作成**:
   - `package.json.example`を参考に`package.json`を作成
   - `exports`フィールドで型定義をエクスポート

2. **型定義の共有設定の確認**:
   - `apps/expo/tsconfig.json`のパスエイリアスの確認
   - `apps/admin/tsconfig.json`のパスエイリアスの確認
   - `apps/www/tsconfig.json`のパスエイリアスの確認（存在する場合）

3. **開発環境の動作確認**:
   - `pnpm install`で依存関係をインストール
   - `pnpm turbo dev`で開発環境を起動
   - `convex dev`が正常に動作することを確認

---

## 結論

**現在の設計は概ね良好ですが、以下の設定ファイルの確認・作成が必要です：**

1. ✅ **ディレクトリ構成**: 問題なし
2. ⚠️ **`turbo.json`**: 作成が必要（`convex dev`のキャッシュ対策）
3. ⚠️ **`pnpm-workspace.yaml`**: 確認が必要（ワークスペース設定）
4. ⚠️ **`convex.json`**: 確認が必要（Convex設定）
5. ✅ **型定義の共有**: 現在の設計で対応可能

**提供された意見は正しく、現在の設計もそれに沿っています。ただし、いくつかの設定ファイルの確認・作成が必要です。**

---

## 作成・更新したファイル

### 設定ファイル（新規作成）

1. **`turbo.json`**: Turborepo設定
   - `dev`タスクに`cache: false`を指定（`convex dev`のキャッシュ対策）
   - `persistent: true`で常時実行を明示

2. **`pnpm-workspace.yaml`**: pnpmワークスペース設定
   - `packages/backend/`を独立したワークスペースとして定義

3. **`packages/backend/convex.json`**: Convex設定ファイル
   - Convex関数のディレクトリを指定
   - ES modulesを使用

4. **`packages/backend/package.json.example`**: package.jsonテンプレート
   - `exports`フィールドで型定義をエクスポート
   - Convex専用の依存関係を管理

### ドキュメント（新規作成・更新）

1. **`DESIGN_REVIEW_MISE_CONVEX_TURBOREPO.md`**: 設計レビュー結果（このファイル）
2. **`.cursor/rules/TURBOREPO_CONVEX.md`**: Turborepo + Convex統合ガイド
3. **`.cursor/rules/PROJECT.md`**: ConvexとReact NativeのNode環境の分離について追記

## 参考資料

- [.cursor/rules/PROJECT.md](./.cursor/rules/PROJECT.md): プロジェクトルール
- [.cursor/rules/TURBOREPO_CONVEX.md](./.cursor/rules/TURBOREPO_CONVEX.md): Turborepo + Convex統合ガイド ⭐ **新規作成**
- [.cursor/skills/package-structure/SKILL.md](./.cursor/skills/package-structure/SKILL.md): パッケージ構造スキル
- [.cursor/skills/convex-patterns/SKILL.md](./.cursor/skills/convex-patterns/SKILL.md): Convex開発パターン
- [MISE_SETUP.md](./MISE_SETUP.md): miseセットアップガイド
